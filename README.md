## Description
A simple back-end user registration and login system built with NestJS (backend), and MongoDB.

## Project setup

```bash
$ npm install
```

Create a .env file:

MONGO_URI=mongodb://localhost:27017/{yourdb}
PORT=3000

## Compile and run the project

```bash
# development
$ npm run start
